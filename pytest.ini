[pytest]
testpaths = tests
addopts = -v
pythonpath = .
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests as requiring model loading (deselect with '-m "not integration"')
